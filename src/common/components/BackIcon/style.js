import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 30,    
    paddingLeft: 15,
  },
  image: {
    width: 12,
    height: 20
  }
});
export default styles;
